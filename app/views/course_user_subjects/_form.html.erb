<%= form_with(model: course_user_subject, local: true) do |form| %>
  <% if course_user_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_user_subject.errors.count, "error") %> prohibited this course_user_subject from being saved:</h2>

      <ul>
      <% course_user_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
